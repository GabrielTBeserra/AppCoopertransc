import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  Card: {
    backgroundColor: '#fff',
    borderRadius: 4,
    borderWidth: 0.5,
    borderColor: '#000',
    alignSelf: 'stretch',
    padding: 10,
    margin: 10,
  },
});

export default styles;
